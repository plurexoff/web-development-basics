# Задание №6: Веб-разработка – основы HTML/CSS/JS

Учебный мини-проект для освоения базовой фронтенд‑разработки:
- HTML – структура страницы
- CSS – оформление и адаптивность
- JavaScript – интерактивность и работа с DOM

## 🎯 Цели задания

1. Создать HTML‑страницу с заголовками, абзацами и списками.
2. Оформить внешний вид страницы с помощью CSS.
3. Добавить интерактивность на странице с помощью JavaScript:
   - обработка кликов;
   - изменение содержимого и стилей элементов;
   - простые динамические эффекты.

## 📂 Структура проекта

```
web-development-basics/
├── index.html          # Главная страница
├── styles.css          # Стили
├── script.js           # Скрипты
├── README.md           # Описание проекта
└── assets/
    └── images/         # (опционально) картинки
```

## 🚀 Быстрый старт

### Вариант 1: Открыть в браузере

1. Склонировать репозиторий или скачать ZIP:
   ```bash
   git clone https://github.com/plurexoff/web-development-basics.git
   cd web-development-basics
   ```
2. Открыть файл `index.html` двойным кликом или через браузер:
   ```
   file:///путь/к/папке/index.html
   ```

### Вариант 2: Локальный сервер (рекомендуется)

```bash
# Python 3
python -m http.server 8000
# или
python -m http.server 8080
```

Затем открыть в браузере:

```
http://localhost:8000
```

## 🧱 Что реализовано

- Семантическая разметка: `header`, `main`, `section`, `article`, `footer`.
- Заголовки (`h1–h3`), абзацы (`p`), маркированные и нумерованные списки (`ul`, `ol`).
- Базовая типографика и современный адаптивный layout на Flexbox.
- Тематический блок «План обучения фронтенду».
- Интерактивные элементы на JavaScript:
  - кнопка «Показать/скрыть план»;
  - счётчик кликов;
  - динамическая смена темы (светлая/тёмная);
  - изменение текста и стилей при действиях пользователя.

## 🧩 JavaScript‑функциональность

В `script.js` реализовано:

- Работа с DOM через `document.querySelector`/`querySelectorAll`.
- Обработка событий `click`.
- Манипуляции с классами (`classList.add/remove/toggle`).
- Изменение текста через `textContent` / `innerHTML`.

## 📚 Что стоит повторить

- Теги: `h1–h6`, `p`, `a`, `ul`, `ol`, `li`, `div`, `span`, `header`, `main`, `section`, `article`, `footer`.
- Основы CSS: селекторы, классы, id, `margin`, `padding`, `border`, `display`, `flex`, цвета, шрифты.
- Базовый JavaScript: переменные, функции, события, работа с DOM.

## ✅ Как можно доработать

- Добавить форму обратной связи (`<form>`, `<input>`, `<textarea>`, `<button>`).
- Сделать всплывающее модальное окно на JS.
- Добавить анимации через CSS `transition`/`@keyframes`.
- Подключить Google Fonts.

---

Автор: **plurexoff**
